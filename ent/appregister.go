// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"glintfed.org/ent/appregister"
)

// AppRegister is the model entity for the AppRegister schema.
type AppRegister struct {
	config `json:"-"`
	// ID of the ent.
	ID uint64 `json:"id,omitempty"`
	// Email holds the value of the "email" field.
	Email string `json:"email,omitempty"`
	// VerifyCode holds the value of the "verify_code" field.
	VerifyCode string `json:"verify_code,omitempty"`
	// EmailDeliveredAt holds the value of the "email_delivered_at" field.
	EmailDeliveredAt time.Time `json:"email_delivered_at,omitempty"`
	// EmailVerifiedAt holds the value of the "email_verified_at" field.
	EmailVerifiedAt time.Time `json:"email_verified_at,omitempty"`
	// Uses holds the value of the "uses" field.
	Uses uint32 `json:"uses,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt    time.Time `json:"updated_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*AppRegister) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case appregister.FieldID, appregister.FieldUses:
			values[i] = new(sql.NullInt64)
		case appregister.FieldEmail, appregister.FieldVerifyCode:
			values[i] = new(sql.NullString)
		case appregister.FieldEmailDeliveredAt, appregister.FieldEmailVerifiedAt, appregister.FieldCreatedAt, appregister.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the AppRegister fields.
func (_m *AppRegister) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case appregister.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = uint64(value.Int64)
		case appregister.FieldEmail:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field email", values[i])
			} else if value.Valid {
				_m.Email = value.String
			}
		case appregister.FieldVerifyCode:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field verify_code", values[i])
			} else if value.Valid {
				_m.VerifyCode = value.String
			}
		case appregister.FieldEmailDeliveredAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field email_delivered_at", values[i])
			} else if value.Valid {
				_m.EmailDeliveredAt = value.Time
			}
		case appregister.FieldEmailVerifiedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field email_verified_at", values[i])
			} else if value.Valid {
				_m.EmailVerifiedAt = value.Time
			}
		case appregister.FieldUses:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field uses", values[i])
			} else if value.Valid {
				_m.Uses = uint32(value.Int64)
			}
		case appregister.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case appregister.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the AppRegister.
// This includes values selected through modifiers, order, etc.
func (_m *AppRegister) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this AppRegister.
// Note that you need to call AppRegister.Unwrap() before calling this method if this AppRegister
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *AppRegister) Update() *AppRegisterUpdateOne {
	return NewAppRegisterClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the AppRegister entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *AppRegister) Unwrap() *AppRegister {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: AppRegister is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *AppRegister) String() string {
	var builder strings.Builder
	builder.WriteString("AppRegister(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("email=")
	builder.WriteString(_m.Email)
	builder.WriteString(", ")
	builder.WriteString("verify_code=")
	builder.WriteString(_m.VerifyCode)
	builder.WriteString(", ")
	builder.WriteString("email_delivered_at=")
	builder.WriteString(_m.EmailDeliveredAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("email_verified_at=")
	builder.WriteString(_m.EmailVerifiedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("uses=")
	builder.WriteString(fmt.Sprintf("%v", _m.Uses))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// AppRegisters is a parsable slice of AppRegister.
type AppRegisters []*AppRegister
