// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"sync"

	"entgo.io/ent/dialect"
)

// Tx is a transactional client that is created by calling Client.Tx().
type Tx struct {
	config
	// AccountInterstitial is the client for interacting with the AccountInterstitial builders.
	AccountInterstitial *AccountInterstitialClient
	// AccountLog is the client for interacting with the AccountLog builders.
	AccountLog *AccountLogClient
	// Activity is the client for interacting with the Activity builders.
	Activity *ActivityClient
	// AdminInvite is the client for interacting with the AdminInvite builders.
	AdminInvite *AdminInviteClient
	// AdminShadowFilter is the client for interacting with the AdminShadowFilter builders.
	AdminShadowFilter *AdminShadowFilterClient
	// AppRegister is the client for interacting with the AppRegister builders.
	AppRegister *AppRegisterClient
	// AutospamCustomTokens is the client for interacting with the AutospamCustomTokens builders.
	AutospamCustomTokens *AutospamCustomTokensClient
	// Avatar is the client for interacting with the Avatar builders.
	Avatar *AvatarClient
	// Bookmark is the client for interacting with the Bookmark builders.
	Bookmark *BookmarkClient
	// Circle is the client for interacting with the Circle builders.
	Circle *CircleClient
	// CircleProfile is the client for interacting with the CircleProfile builders.
	CircleProfile *CircleProfileClient
	// Collection is the client for interacting with the Collection builders.
	Collection *CollectionClient
	// CollectionItem is the client for interacting with the CollectionItem builders.
	CollectionItem *CollectionItemClient
	// Comment is the client for interacting with the Comment builders.
	Comment *CommentClient
	// ConfigCache is the client for interacting with the ConfigCache builders.
	ConfigCache *ConfigCacheClient
	// Contact is the client for interacting with the Contact builders.
	Contact *ContactClient
	// Conversation is the client for interacting with the Conversation builders.
	Conversation *ConversationClient
	// CuratedRegister is the client for interacting with the CuratedRegister builders.
	CuratedRegister *CuratedRegisterClient
	// CuratedRegisterActivity is the client for interacting with the CuratedRegisterActivity builders.
	CuratedRegisterActivity *CuratedRegisterActivityClient
	// CuratedRegisterTemplate is the client for interacting with the CuratedRegisterTemplate builders.
	CuratedRegisterTemplate *CuratedRegisterTemplateClient
	// CustomEmoji is the client for interacting with the CustomEmoji builders.
	CustomEmoji *CustomEmojiClient
	// CustomFilter is the client for interacting with the CustomFilter builders.
	CustomFilter *CustomFilterClient
	// CustomFilterKeyword is the client for interacting with the CustomFilterKeyword builders.
	CustomFilterKeyword *CustomFilterKeywordClient
	// CustomFilterStatus is the client for interacting with the CustomFilterStatus builders.
	CustomFilterStatus *CustomFilterStatusClient
	// DefaultDomainBlock is the client for interacting with the DefaultDomainBlock builders.
	DefaultDomainBlock *DefaultDomainBlockClient
	// DirectMessage is the client for interacting with the DirectMessage builders.
	DirectMessage *DirectMessageClient
	// DiscoverCategory is the client for interacting with the DiscoverCategory builders.
	DiscoverCategory *DiscoverCategoryClient
	// DiscoverCategoryHashtag is the client for interacting with the DiscoverCategoryHashtag builders.
	DiscoverCategoryHashtag *DiscoverCategoryHashtagClient
	// EmailVerification is the client for interacting with the EmailVerification builders.
	EmailVerification *EmailVerificationClient
	// FailedJob is the client for interacting with the FailedJob builders.
	FailedJob *FailedJobClient
	// FollowRequest is the client for interacting with the FollowRequest builders.
	FollowRequest *FollowRequestClient
	// Follower is the client for interacting with the Follower builders.
	Follower *FollowerClient
	// Group is the client for interacting with the Group builders.
	Group *GroupClient
	// GroupActivityGraph is the client for interacting with the GroupActivityGraph builders.
	GroupActivityGraph *GroupActivityGraphClient
	// GroupBlock is the client for interacting with the GroupBlock builders.
	GroupBlock *GroupBlockClient
	// GroupCategory is the client for interacting with the GroupCategory builders.
	GroupCategory *GroupCategoryClient
	// GroupComment is the client for interacting with the GroupComment builders.
	GroupComment *GroupCommentClient
	// GroupEvent is the client for interacting with the GroupEvent builders.
	GroupEvent *GroupEventClient
	// GroupHashtag is the client for interacting with the GroupHashtag builders.
	GroupHashtag *GroupHashtagClient
	// GroupInteraction is the client for interacting with the GroupInteraction builders.
	GroupInteraction *GroupInteractionClient
	// GroupInvitation is the client for interacting with the GroupInvitation builders.
	GroupInvitation *GroupInvitationClient
	// GroupLike is the client for interacting with the GroupLike builders.
	GroupLike *GroupLikeClient
	// GroupLimit is the client for interacting with the GroupLimit builders.
	GroupLimit *GroupLimitClient
	// GroupMedia is the client for interacting with the GroupMedia builders.
	GroupMedia *GroupMediaClient
	// GroupMember is the client for interacting with the GroupMember builders.
	GroupMember *GroupMemberClient
	// GroupPost is the client for interacting with the GroupPost builders.
	GroupPost *GroupPostClient
	// GroupPostHashtag is the client for interacting with the GroupPostHashtag builders.
	GroupPostHashtag *GroupPostHashtagClient
	// GroupReport is the client for interacting with the GroupReport builders.
	GroupReport *GroupReportClient
	// GroupRole is the client for interacting with the GroupRole builders.
	GroupRole *GroupRoleClient
	// GroupStore is the client for interacting with the GroupStore builders.
	GroupStore *GroupStoreClient
	// Hashtag is the client for interacting with the Hashtag builders.
	Hashtag *HashtagClient
	// HashtagFollow is the client for interacting with the HashtagFollow builders.
	HashtagFollow *HashtagFollowClient
	// HashtagRelated is the client for interacting with the HashtagRelated builders.
	HashtagRelated *HashtagRelatedClient
	// ImportData is the client for interacting with the ImportData builders.
	ImportData *ImportDataClient
	// ImportJob is the client for interacting with the ImportJob builders.
	ImportJob *ImportJobClient
	// ImportPost is the client for interacting with the ImportPost builders.
	ImportPost *ImportPostClient
	// Instance is the client for interacting with the Instance builders.
	Instance *InstanceClient
	// InstanceActor is the client for interacting with the InstanceActor builders.
	InstanceActor *InstanceActorClient
	// Like is the client for interacting with the Like builders.
	Like *LikeClient
	// LiveStream is the client for interacting with the LiveStream builders.
	LiveStream *LiveStreamClient
	// Media is the client for interacting with the Media builders.
	Media *MediaClient
	// MediaBlocklist is the client for interacting with the MediaBlocklist builders.
	MediaBlocklist *MediaBlocklistClient
	// MediaTag is the client for interacting with the MediaTag builders.
	MediaTag *MediaTagClient
	// Mention is the client for interacting with the Mention builders.
	Mention *MentionClient
	// ModLog is the client for interacting with the ModLog builders.
	ModLog *ModLogClient
	// ModeratedProfile is the client for interacting with the ModeratedProfile builders.
	ModeratedProfile *ModeratedProfileClient
	// Newsroom is the client for interacting with the Newsroom builders.
	Newsroom *NewsroomClient
	// Notification is the client for interacting with the Notification builders.
	Notification *NotificationClient
	// OauthAccessToken is the client for interacting with the OauthAccessToken builders.
	OauthAccessToken *OauthAccessTokenClient
	// OauthAuthorizationCode is the client for interacting with the OauthAuthorizationCode builders.
	OauthAuthorizationCode *OauthAuthorizationCodeClient
	// OauthClient is the client for interacting with the OauthClient builders.
	OauthClient *OauthClientClient
	// OauthPersonalAccessClient is the client for interacting with the OauthPersonalAccessClient builders.
	OauthPersonalAccessClient *OauthPersonalAccessClientClient
	// OauthPkce is the client for interacting with the OauthPkce builders.
	OauthPkce *OauthPkceClient
	// OauthRefreshToken is the client for interacting with the OauthRefreshToken builders.
	OauthRefreshToken *OauthRefreshTokenClient
	// Page is the client for interacting with the Page builders.
	Page *PageClient
	// ParentalControls is the client for interacting with the ParentalControls builders.
	ParentalControls *ParentalControlsClient
	// Place is the client for interacting with the Place builders.
	Place *PlaceClient
	// Poll is the client for interacting with the Poll builders.
	Poll *PollClient
	// PollVote is the client for interacting with the PollVote builders.
	PollVote *PollVoteClient
	// Portfolio is the client for interacting with the Portfolio builders.
	Portfolio *PortfolioClient
	// Profile is the client for interacting with the Profile builders.
	Profile *ProfileClient
	// ProfileAlias is the client for interacting with the ProfileAlias builders.
	ProfileAlias *ProfileAliasClient
	// ProfileMigration is the client for interacting with the ProfileMigration builders.
	ProfileMigration *ProfileMigrationClient
	// ProfileSponsor is the client for interacting with the ProfileSponsor builders.
	ProfileSponsor *ProfileSponsorClient
	// RemoteAuth is the client for interacting with the RemoteAuth builders.
	RemoteAuth *RemoteAuthClient
	// RemoteAuthInstance is the client for interacting with the RemoteAuthInstance builders.
	RemoteAuthInstance *RemoteAuthInstanceClient
	// RemoteReport is the client for interacting with the RemoteReport builders.
	RemoteReport *RemoteReportClient
	// Report is the client for interacting with the Report builders.
	Report *ReportClient
	// ReportComment is the client for interacting with the ReportComment builders.
	ReportComment *ReportCommentClient
	// ReportLog is the client for interacting with the ReportLog builders.
	ReportLog *ReportLogClient
	// Status is the client for interacting with the Status builders.
	Status *StatusClient
	// StatusArchived is the client for interacting with the StatusArchived builders.
	StatusArchived *StatusArchivedClient
	// StatusEdit is the client for interacting with the StatusEdit builders.
	StatusEdit *StatusEditClient
	// StatusHashtag is the client for interacting with the StatusHashtag builders.
	StatusHashtag *StatusHashtagClient
	// StatusView is the client for interacting with the StatusView builders.
	StatusView *StatusViewClient
	// Story is the client for interacting with the Story builders.
	Story *StoryClient
	// StoryItem is the client for interacting with the StoryItem builders.
	StoryItem *StoryItemClient
	// StoryReaction is the client for interacting with the StoryReaction builders.
	StoryReaction *StoryReactionClient
	// StoryView is the client for interacting with the StoryView builders.
	StoryView *StoryViewClient
	// UIKit is the client for interacting with the UIKit builders.
	UIKit *UIKitClient
	// User is the client for interacting with the User builders.
	User *UserClient
	// UserAppSettings is the client for interacting with the UserAppSettings builders.
	UserAppSettings *UserAppSettingsClient
	// UserDevice is the client for interacting with the UserDevice builders.
	UserDevice *UserDeviceClient
	// UserDomainBlock is the client for interacting with the UserDomainBlock builders.
	UserDomainBlock *UserDomainBlockClient
	// UserEmailForgot is the client for interacting with the UserEmailForgot builders.
	UserEmailForgot *UserEmailForgotClient
	// UserFilter is the client for interacting with the UserFilter builders.
	UserFilter *UserFilterClient
	// UserInvite is the client for interacting with the UserInvite builders.
	UserInvite *UserInviteClient
	// UserOidcMapping is the client for interacting with the UserOidcMapping builders.
	UserOidcMapping *UserOidcMappingClient
	// UserPronoun is the client for interacting with the UserPronoun builders.
	UserPronoun *UserPronounClient
	// UserRoles is the client for interacting with the UserRoles builders.
	UserRoles *UserRolesClient
	// UserSetting is the client for interacting with the UserSetting builders.
	UserSetting *UserSettingClient

	// lazily loaded.
	client     *Client
	clientOnce sync.Once
	// ctx lives for the life of the transaction. It is
	// the same context used by the underlying connection.
	ctx context.Context
}

type (
	// Committer is the interface that wraps the Commit method.
	Committer interface {
		Commit(context.Context, *Tx) error
	}

	// The CommitFunc type is an adapter to allow the use of ordinary
	// function as a Committer. If f is a function with the appropriate
	// signature, CommitFunc(f) is a Committer that calls f.
	CommitFunc func(context.Context, *Tx) error

	// CommitHook defines the "commit middleware". A function that gets a Committer
	// and returns a Committer. For example:
	//
	//	hook := func(next ent.Committer) ent.Committer {
	//		return ent.CommitFunc(func(ctx context.Context, tx *ent.Tx) error {
	//			// Do some stuff before.
	//			if err := next.Commit(ctx, tx); err != nil {
	//				return err
	//			}
	//			// Do some stuff after.
	//			return nil
	//		})
	//	}
	//
	CommitHook func(Committer) Committer
)

// Commit calls f(ctx, m).
func (f CommitFunc) Commit(ctx context.Context, tx *Tx) error {
	return f(ctx, tx)
}

// Commit commits the transaction.
func (tx *Tx) Commit() error {
	txDriver := tx.config.driver.(*txDriver)
	var fn Committer = CommitFunc(func(context.Context, *Tx) error {
		return txDriver.tx.Commit()
	})
	txDriver.mu.Lock()
	hooks := append([]CommitHook(nil), txDriver.onCommit...)
	txDriver.mu.Unlock()
	for i := len(hooks) - 1; i >= 0; i-- {
		fn = hooks[i](fn)
	}
	return fn.Commit(tx.ctx, tx)
}

// OnCommit adds a hook to call on commit.
func (tx *Tx) OnCommit(f CommitHook) {
	txDriver := tx.config.driver.(*txDriver)
	txDriver.mu.Lock()
	txDriver.onCommit = append(txDriver.onCommit, f)
	txDriver.mu.Unlock()
}

type (
	// Rollbacker is the interface that wraps the Rollback method.
	Rollbacker interface {
		Rollback(context.Context, *Tx) error
	}

	// The RollbackFunc type is an adapter to allow the use of ordinary
	// function as a Rollbacker. If f is a function with the appropriate
	// signature, RollbackFunc(f) is a Rollbacker that calls f.
	RollbackFunc func(context.Context, *Tx) error

	// RollbackHook defines the "rollback middleware". A function that gets a Rollbacker
	// and returns a Rollbacker. For example:
	//
	//	hook := func(next ent.Rollbacker) ent.Rollbacker {
	//		return ent.RollbackFunc(func(ctx context.Context, tx *ent.Tx) error {
	//			// Do some stuff before.
	//			if err := next.Rollback(ctx, tx); err != nil {
	//				return err
	//			}
	//			// Do some stuff after.
	//			return nil
	//		})
	//	}
	//
	RollbackHook func(Rollbacker) Rollbacker
)

// Rollback calls f(ctx, m).
func (f RollbackFunc) Rollback(ctx context.Context, tx *Tx) error {
	return f(ctx, tx)
}

// Rollback rollbacks the transaction.
func (tx *Tx) Rollback() error {
	txDriver := tx.config.driver.(*txDriver)
	var fn Rollbacker = RollbackFunc(func(context.Context, *Tx) error {
		return txDriver.tx.Rollback()
	})
	txDriver.mu.Lock()
	hooks := append([]RollbackHook(nil), txDriver.onRollback...)
	txDriver.mu.Unlock()
	for i := len(hooks) - 1; i >= 0; i-- {
		fn = hooks[i](fn)
	}
	return fn.Rollback(tx.ctx, tx)
}

// OnRollback adds a hook to call on rollback.
func (tx *Tx) OnRollback(f RollbackHook) {
	txDriver := tx.config.driver.(*txDriver)
	txDriver.mu.Lock()
	txDriver.onRollback = append(txDriver.onRollback, f)
	txDriver.mu.Unlock()
}

// Client returns a Client that binds to current transaction.
func (tx *Tx) Client() *Client {
	tx.clientOnce.Do(func() {
		tx.client = &Client{config: tx.config}
		tx.client.init()
	})
	return tx.client
}

func (tx *Tx) init() {
	tx.AccountInterstitial = NewAccountInterstitialClient(tx.config)
	tx.AccountLog = NewAccountLogClient(tx.config)
	tx.Activity = NewActivityClient(tx.config)
	tx.AdminInvite = NewAdminInviteClient(tx.config)
	tx.AdminShadowFilter = NewAdminShadowFilterClient(tx.config)
	tx.AppRegister = NewAppRegisterClient(tx.config)
	tx.AutospamCustomTokens = NewAutospamCustomTokensClient(tx.config)
	tx.Avatar = NewAvatarClient(tx.config)
	tx.Bookmark = NewBookmarkClient(tx.config)
	tx.Circle = NewCircleClient(tx.config)
	tx.CircleProfile = NewCircleProfileClient(tx.config)
	tx.Collection = NewCollectionClient(tx.config)
	tx.CollectionItem = NewCollectionItemClient(tx.config)
	tx.Comment = NewCommentClient(tx.config)
	tx.ConfigCache = NewConfigCacheClient(tx.config)
	tx.Contact = NewContactClient(tx.config)
	tx.Conversation = NewConversationClient(tx.config)
	tx.CuratedRegister = NewCuratedRegisterClient(tx.config)
	tx.CuratedRegisterActivity = NewCuratedRegisterActivityClient(tx.config)
	tx.CuratedRegisterTemplate = NewCuratedRegisterTemplateClient(tx.config)
	tx.CustomEmoji = NewCustomEmojiClient(tx.config)
	tx.CustomFilter = NewCustomFilterClient(tx.config)
	tx.CustomFilterKeyword = NewCustomFilterKeywordClient(tx.config)
	tx.CustomFilterStatus = NewCustomFilterStatusClient(tx.config)
	tx.DefaultDomainBlock = NewDefaultDomainBlockClient(tx.config)
	tx.DirectMessage = NewDirectMessageClient(tx.config)
	tx.DiscoverCategory = NewDiscoverCategoryClient(tx.config)
	tx.DiscoverCategoryHashtag = NewDiscoverCategoryHashtagClient(tx.config)
	tx.EmailVerification = NewEmailVerificationClient(tx.config)
	tx.FailedJob = NewFailedJobClient(tx.config)
	tx.FollowRequest = NewFollowRequestClient(tx.config)
	tx.Follower = NewFollowerClient(tx.config)
	tx.Group = NewGroupClient(tx.config)
	tx.GroupActivityGraph = NewGroupActivityGraphClient(tx.config)
	tx.GroupBlock = NewGroupBlockClient(tx.config)
	tx.GroupCategory = NewGroupCategoryClient(tx.config)
	tx.GroupComment = NewGroupCommentClient(tx.config)
	tx.GroupEvent = NewGroupEventClient(tx.config)
	tx.GroupHashtag = NewGroupHashtagClient(tx.config)
	tx.GroupInteraction = NewGroupInteractionClient(tx.config)
	tx.GroupInvitation = NewGroupInvitationClient(tx.config)
	tx.GroupLike = NewGroupLikeClient(tx.config)
	tx.GroupLimit = NewGroupLimitClient(tx.config)
	tx.GroupMedia = NewGroupMediaClient(tx.config)
	tx.GroupMember = NewGroupMemberClient(tx.config)
	tx.GroupPost = NewGroupPostClient(tx.config)
	tx.GroupPostHashtag = NewGroupPostHashtagClient(tx.config)
	tx.GroupReport = NewGroupReportClient(tx.config)
	tx.GroupRole = NewGroupRoleClient(tx.config)
	tx.GroupStore = NewGroupStoreClient(tx.config)
	tx.Hashtag = NewHashtagClient(tx.config)
	tx.HashtagFollow = NewHashtagFollowClient(tx.config)
	tx.HashtagRelated = NewHashtagRelatedClient(tx.config)
	tx.ImportData = NewImportDataClient(tx.config)
	tx.ImportJob = NewImportJobClient(tx.config)
	tx.ImportPost = NewImportPostClient(tx.config)
	tx.Instance = NewInstanceClient(tx.config)
	tx.InstanceActor = NewInstanceActorClient(tx.config)
	tx.Like = NewLikeClient(tx.config)
	tx.LiveStream = NewLiveStreamClient(tx.config)
	tx.Media = NewMediaClient(tx.config)
	tx.MediaBlocklist = NewMediaBlocklistClient(tx.config)
	tx.MediaTag = NewMediaTagClient(tx.config)
	tx.Mention = NewMentionClient(tx.config)
	tx.ModLog = NewModLogClient(tx.config)
	tx.ModeratedProfile = NewModeratedProfileClient(tx.config)
	tx.Newsroom = NewNewsroomClient(tx.config)
	tx.Notification = NewNotificationClient(tx.config)
	tx.OauthAccessToken = NewOauthAccessTokenClient(tx.config)
	tx.OauthAuthorizationCode = NewOauthAuthorizationCodeClient(tx.config)
	tx.OauthClient = NewOauthClientClient(tx.config)
	tx.OauthPersonalAccessClient = NewOauthPersonalAccessClientClient(tx.config)
	tx.OauthPkce = NewOauthPkceClient(tx.config)
	tx.OauthRefreshToken = NewOauthRefreshTokenClient(tx.config)
	tx.Page = NewPageClient(tx.config)
	tx.ParentalControls = NewParentalControlsClient(tx.config)
	tx.Place = NewPlaceClient(tx.config)
	tx.Poll = NewPollClient(tx.config)
	tx.PollVote = NewPollVoteClient(tx.config)
	tx.Portfolio = NewPortfolioClient(tx.config)
	tx.Profile = NewProfileClient(tx.config)
	tx.ProfileAlias = NewProfileAliasClient(tx.config)
	tx.ProfileMigration = NewProfileMigrationClient(tx.config)
	tx.ProfileSponsor = NewProfileSponsorClient(tx.config)
	tx.RemoteAuth = NewRemoteAuthClient(tx.config)
	tx.RemoteAuthInstance = NewRemoteAuthInstanceClient(tx.config)
	tx.RemoteReport = NewRemoteReportClient(tx.config)
	tx.Report = NewReportClient(tx.config)
	tx.ReportComment = NewReportCommentClient(tx.config)
	tx.ReportLog = NewReportLogClient(tx.config)
	tx.Status = NewStatusClient(tx.config)
	tx.StatusArchived = NewStatusArchivedClient(tx.config)
	tx.StatusEdit = NewStatusEditClient(tx.config)
	tx.StatusHashtag = NewStatusHashtagClient(tx.config)
	tx.StatusView = NewStatusViewClient(tx.config)
	tx.Story = NewStoryClient(tx.config)
	tx.StoryItem = NewStoryItemClient(tx.config)
	tx.StoryReaction = NewStoryReactionClient(tx.config)
	tx.StoryView = NewStoryViewClient(tx.config)
	tx.UIKit = NewUIKitClient(tx.config)
	tx.User = NewUserClient(tx.config)
	tx.UserAppSettings = NewUserAppSettingsClient(tx.config)
	tx.UserDevice = NewUserDeviceClient(tx.config)
	tx.UserDomainBlock = NewUserDomainBlockClient(tx.config)
	tx.UserEmailForgot = NewUserEmailForgotClient(tx.config)
	tx.UserFilter = NewUserFilterClient(tx.config)
	tx.UserInvite = NewUserInviteClient(tx.config)
	tx.UserOidcMapping = NewUserOidcMappingClient(tx.config)
	tx.UserPronoun = NewUserPronounClient(tx.config)
	tx.UserRoles = NewUserRolesClient(tx.config)
	tx.UserSetting = NewUserSettingClient(tx.config)
}

// txDriver wraps the given dialect.Tx with a nop dialect.Driver implementation.
// The idea is to support transactions without adding any extra code to the builders.
// When a builder calls to driver.Tx(), it gets the same dialect.Tx instance.
// Commit and Rollback are nop for the internal builders and the user must call one
// of them in order to commit or rollback the transaction.
//
// If a closed transaction is embedded in one of the generated entities, and the entity
// applies a query, for example: AccountInterstitial.QueryXXX(), the query will be executed
// through the driver which created this transaction.
//
// Note that txDriver is not goroutine safe.
type txDriver struct {
	// the driver we started the transaction from.
	drv dialect.Driver
	// tx is the underlying transaction.
	tx dialect.Tx
	// completion hooks.
	mu         sync.Mutex
	onCommit   []CommitHook
	onRollback []RollbackHook
}

// newTx creates a new transactional driver.
func newTx(ctx context.Context, drv dialect.Driver) (*txDriver, error) {
	tx, err := drv.Tx(ctx)
	if err != nil {
		return nil, err
	}
	return &txDriver{tx: tx, drv: drv}, nil
}

// Tx returns the transaction wrapper (txDriver) to avoid Commit or Rollback calls
// from the internal builders. Should be called only by the internal builders.
func (tx *txDriver) Tx(context.Context) (dialect.Tx, error) { return tx, nil }

// Dialect returns the dialect of the driver we started the transaction from.
func (tx *txDriver) Dialect() string { return tx.drv.Dialect() }

// Close is a nop close.
func (*txDriver) Close() error { return nil }

// Commit is a nop commit for the internal builders.
// User must call `Tx.Commit` in order to commit the transaction.
func (*txDriver) Commit() error { return nil }

// Rollback is a nop rollback for the internal builders.
// User must call `Tx.Rollback` in order to rollback the transaction.
func (*txDriver) Rollback() error { return nil }

// Exec calls tx.Exec.
func (tx *txDriver) Exec(ctx context.Context, query string, args, v any) error {
	return tx.tx.Exec(ctx, query, args, v)
}

// Query calls tx.Query.
func (tx *txDriver) Query(ctx context.Context, query string, args, v any) error {
	return tx.tx.Query(ctx, query, args, v)
}

var _ dialect.Driver = (*txDriver)(nil)
